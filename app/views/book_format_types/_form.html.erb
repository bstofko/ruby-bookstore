<%= form_with(model: book_format_type) do |form| %>
  <% if book_format_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book_format_type.errors.count, "error") %> prohibited this book_format_type from being saved:</h2>

      <ul>
        <% book_format_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :physical %>
    <%= form.check_box :physical %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
